from .math import sum

# used so that we tell python that this is a MODULE, 
# so that we can easily take the functions out of the helper folder,
# and import them all once with:
# import helper as "..."
# and use it as "...".function